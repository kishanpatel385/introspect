{
  "name": "@introspect/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI enrichment layer — pluggable providers (Groq, Claude, OpenAI)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@introspect/core-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
