{
  "name": "@introspect/scanner",
  "version": "0.1.0",
  "private": true,
  "description": "Core scanning engine — deterministic code analysis across all languages",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@introspect/core-types": "workspace:*",
    "fast-glob": "^3.3.0",
    "js-yaml": "^4.1.0",
    "simple-git": "^3.27.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
