{
  "name": "@introspect/core-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript interfaces for Introspect",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
